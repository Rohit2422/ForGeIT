import fs from "fs";
import { join } from "path";
import tsConfigTemp from "../assets/tsconfig-temp.js";
export function createTsConfigJson(destination) {
    fs.writeFileSync(join(destination, "tsconfig.json"), JSON.stringify(tsConfigTemp, null, 2));
}
